

Um-Para-Um:
	1 - Unidirecional:
	- Ir na classe que mapeia a FK no banco
	- Adicionar o atributo que referencia a classe do relacionamento e adicionar a anotação @OneToOne
	- (opcional) Adicionar o @JoinColumnn para configurar a coluna da FK
	
	2 - Bidirecional:
	- Ir na classe que não mapeia a FK
	- Adicionar o atributo que referencia a classe do relacionamento
	- Adicionar a anotação @OneToOne com o mappedBy, com o nome do atributo mapeado em 1
	
Muitos-Para-Um:
	1 - Unidirecional:
	- Ir na classe que mapeia a FK no banco
	- Adicionar o atributo que referencia a classe do relacionamento e adicionar a anotação @ManyToOne
	- (opcional) Adicionar o @JoinColumnn para configurar a coluna da FK
	
	2 - Bidirecional:
	- Ir na classe que não mapeia a FK
	- Adicionar o atributo (List) que referencia a classe do relacionamento
	- Adicionar a anotação @OneToMany com o mappedBy, com o nome do atributo mapeado em 1